#
# Main pipeline for building and distributing the anofox_optimize extension
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

# OIDC token needed for AWS credentials in the deploy job.
permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true

jobs:
  duckdb-lts-build:
    name: Build extension binaries (DuckDB LTS v1.4.5)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.4-andium
    with:
      duckdb_version: v1.4.5
      ci_tools_version: v1.4-andium
      extension_name: anofox_optimize
      # windows_amd64: the v1.4-andium vcpkg pin (ce613c4) builds openssl 3.5.0
      # from source on this triplet -- there is no entry for
      # x64-windows-static-md-release-vs2019comp in the duckdb binary cache --
      # and that build acquires msys2-runtime 3.5.4-2, a package msys2 has
      # since deleted from every mirror. All six mirrors 404 and `vcpkg
      # install` dies. There is no vcpkg commit that fixes this and keeps the
      # LTS line building: the msys2 script update (Dec 2025) lands after
      # openssl 3.6.0 (Oct 2025), and openssl 3.6.0 does not configure in the
      # v1.4 Linux containers, which took linux_arm64 down when tried. So the
      # LTS line ships without a Windows binary until extension-ci-tools moves
      # its own v1.4 pin; Windows is unaffected on the v1.5 line below.
      # windows_amd64_rtools: only needed for R package builds.
      # windows_amd64_mingw: this extension links openssl through vcpkg (for
      # telemetry) and that toolchain does not provide it reliably. NOTE: this
      # is NOT the Rust/getrandom reason the sibling repos give -- there is no
      # Rust here.
      exclude_archs: 'windows_amd64;windows_amd64_rtools;windows_amd64_mingw'

  duckdb-lts-smoke-test:
    name: Smoke test (v1.4.5)
    needs: duckdb-lts-build
    uses: ./.github/workflows/_extension_smoke_test.yml
    with:
      duckdb_version: v1.4.5
      extension_name: anofox_optimize
      exclude_archs: 'windows_amd64;windows_amd64_rtools;windows_amd64_mingw'

  duckdb-lts-deploy:
    name: Deploy extension binaries (v1.4.5 LTS)
    needs: [duckdb-lts-build, duckdb-lts-smoke-test]
    # Run the deploy step even when a non-essential build matrix entry fails.
    # The deploy job itself fans out per arch and only pushes the artifacts
    # that actually exist.
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/_extension_deploy.yml
    secrets: inherit
    with:
      duckdb_version: v1.4.5
      ci_tools_version: v1.4-andium
      extension_name: anofox_optimize
      exclude_archs: 'windows_amd64;windows_amd64_rtools;windows_amd64_mingw'
      deploy_latest: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
      deploy_versioned: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}

  duckdb-latest-build:
    name: Build extension binaries (DuckDB latest v1.5.5)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.5
      ci_tools_version: v1.5-variegata
      extension_name: anofox_optimize
      # windows_amd64_rtools: only needed for R package builds.
      # windows_amd64_mingw: this extension links openssl through vcpkg (for
      # telemetry) and that toolchain does not provide it reliably. NOTE: this
      # is NOT the Rust/getrandom reason the sibling repos give -- there is no
      # Rust here.
      exclude_archs: 'windows_amd64_rtools;windows_amd64_mingw'

  duckdb-latest-smoke-test:
    name: Smoke test (v1.5.5)
    needs: duckdb-latest-build
    uses: ./.github/workflows/_extension_smoke_test.yml
    with:
      duckdb_version: v1.5.5
      extension_name: anofox_optimize
      exclude_archs: 'windows_amd64_rtools;windows_amd64_mingw'

  duckdb-latest-deploy:
    name: Deploy extension binaries (v1.5.5)
    needs: [duckdb-latest-build, duckdb-latest-smoke-test]
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/_extension_deploy.yml
    secrets: inherit
    with:
      duckdb_version: v1.5.5
      ci_tools_version: v1.5-variegata
      extension_name: anofox_optimize
      exclude_archs: 'windows_amd64_rtools;windows_amd64_mingw'
      deploy_latest: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
      deploy_versioned: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
